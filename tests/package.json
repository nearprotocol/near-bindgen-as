{
  "name": "near-bindgen",
  "version": "0.0.0",
  "description": "Tests for near bindings",
  "main": "test.js",
  "scripts": {
    "test": "yarn build:asconfig && yarn test:wasm",
    "test:wasm": "node ./test.js",
    "build:asconfig": "node asconfig.js"
  },
  "dependencies": {
    "near-bindgen-as": "../"
  }
}
